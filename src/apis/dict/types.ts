export interface Dict {
  id: string;
  name: string;
  code: string;
  extra: string;
  description: string;
  parentId: string | null;
  imageId: string | null;
  createdAt: string;
  updatedAt: string;
}
